import {
    Typography,
} from "@mui/material";

/**Pre-login greating page */
export default function DogGreeting() {
    return (
    /* eslint-disable indent */
      <div>
        <Typography variant="h1">Welcome to Doggocom</Typography>
        <Typography variant="h2">You can do anything at Doggocom</Typography>
        <Typography variant="h3">Login to use the dog matching service</Typography>
      </div>
    /* eslint-enable indent */
    );

}